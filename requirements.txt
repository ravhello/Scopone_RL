# Core
numpy>=1.23
torch>=2.1

# RL env
gymnasium>=0.29

# Utilities
tqdm>=4.66
numba>=0.57

# Reporting/benchmark
pandas>=1.5
openpyxl>=3.1

# Testing
pytest>=7.0

# Optional (used with fallback in observation.compute_rank_probabilities_by_player)
scipy>=1.10
tensorboard>=2.12

# Extras
pygame>=2.5
# tmux (system package; install via OS package manager, not via pip)